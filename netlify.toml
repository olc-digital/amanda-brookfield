[build]
  publish = "./public"
  command = "npm run build && npm run build-functions"
  functions = "./built-lambda"
[build.environment]
  YARN_VERSION = "1.12.3"
  YARN_FLAGS = "--no-ignore-optional"